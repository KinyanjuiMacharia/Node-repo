var counter = function(arr){

    return "There are " + arr.length + " elements in the array";
}

//module.exports specifies explicitely which part of this code will be accesible outside this module
module.exports = counter;